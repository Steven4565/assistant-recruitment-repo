
#ifndef GAME_UTILS_H
#define GAME_UTILS_H

#include <stdbool.h>
#include "../structs.h"
#include "../globals.h"

char getBoardChar(Vector2D position)
{
	// if is
	// 	spaceship return spaceship char

	// 			if is enemy return enemy char

	// 			if is bullet return bullet char

	// 			return board char
}

bool checkCollision(Node object1, Node object2)
{
}

#endif